<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Blade Admin | Jonny Blades</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600&family=Manrope:wght@300;500;700&display=swap"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="admin-page">
  <div class="grain"></div>
  <div class="halo"></div>
  <main class="section">
    <div class="section-heading">
      <p class="eyebrow">Blade Admin</p>
      <h1>Site updates, analytics, and rewards</h1>
      <p>Enter your admin token to unlock tools.</p>
    </div>

    <div class="admin-card">
      <label for="admin-token">Admin Token</label>
      <input id="admin-token" type="password" placeholder="Paste BLADE_ADMIN_TOKEN" value="<%= token %>" />
    </div>

    <div class="admin-grid">
      <div class="admin-card">
        <h3>Site content editor</h3>
        <p>Update the search index and quick links for Blade.</p>
        <textarea id="content-json" rows="10" placeholder="Paste sections JSON here"></textarea>
        <button class="btn primary" id="save-content" type="button">Save content</button>
        <p class="note" id="content-status"></p>
      </div>

      <div class="admin-card">
        <h3>Search analytics</h3>
        <p>Top searches from the Blade assistant.</p>
        <button class="btn ghost" id="refresh-analytics" type="button">Refresh</button>
        <ul id="analytics-list"></ul>
      </div>

      <div class="admin-card">
        <h3>Rewards members</h3>
        <p>Recent signups and points balance.</p>
        <button class="btn ghost" id="refresh-members" type="button">Refresh</button>
        <ul id="member-list"></ul>
      </div>

      <div class="admin-card">
        <h3>Add rewards points</h3>
        <p>Reward loyal clients with free visits and grooming.</p>
        <input id="points-email" type="email" placeholder="Member email" />
        <input id="points-value" type="number" placeholder="Points to add" />
        <button class="btn primary" id="add-points" type="button">Add points</button>
        <p class="note" id="points-status"></p>
      </div>
    </div>
  </main>

  <script src="/js/admin.js"></script>
</body>
</html>
